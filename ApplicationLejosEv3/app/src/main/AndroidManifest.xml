<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="androidproject.applicationlejosev3">

    <uses-permission android:name="android.permission.BLUETOOTH"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".connection.ConnectionBluetoothActivity">
        </activity>
        <activity android:name=".connection.ConnectBluetoothActivity">
        </activity>
        <activity android:name=".panel.ControlPageActivity">
        </activity>
        <activity android:name=".connection.ConnectWifiActivity">
        </activity>
    </application>

</manifest>